@model IEnumerable<Models.Continente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdContinente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreContinente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdContinente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreContinente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.IdContinente}) |

                @Html.ActionLink("Delete", "Delete", new {  id=item.IdContinente})
            </td>
        </tr>
}
    </tbody>
</table>
